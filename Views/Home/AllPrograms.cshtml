@model IEnumerable<Resume_Portal.Models.Program>

<<<<<<< HEAD
<main>
    <div class="push-top">
        <span>
            Search for Program:
        </span>
        <input type="text" id="search" name="ser" />
    </div>
    <section class="section_content">

        <div class="main_flex">

            @foreach (var item in Model)
            {
                <div id="flex-ele" class="main_content @(@item.Name.ToString())">
                    <div>
                        <h4>
                            @Html.DisplayFor(modelItem => item.Name)
                        </h4>
                    </div>
                    <span>
                        <img class="img" src="~/program-images/@item.Name/pic/logo.jpg" />
                    </span>
                    <h5>
                        <span>
                            @Html.ActionLink("All Students", "StudentInProgram", new { id = item.Id })
                        </span><span>
                            @Html.ActionLink("Program Details", "ProgramDetails", new { id = item.Id })
                        </span>
                    </h5>
                    <span class="span_img">
                        @Html.DisplayFor(modelItem => item.Discription)
                    </span>


                </div>
            }

        </div>
    </section>
</main>
<script>
    var n = document.querySelector("#search");
    n.addEventListener("change", function (e) {
        var x = document.querySelector("#search").value;
        console.log(x);
        var allPosibles = document.querySelectorAll("#flex-ele");
        if (allPosibles != undefined) {
            for (i = allPosibles.length - 1; i >= 0; i--) {
                if (allPosibles[i].className.includes(x)) {
                    console.log(allPosibles[i].className);
                    allPosibles[i].scrollIntoView();
                    break;
                }
            }
        }
    });

</script>
@Styles.Render("~/Content/Site.css")
<style>
    img {
        width: 20vw;
    }

    h5 {
        color: forestgreen;
    }

    .push-top {
        margin-top: 17vw;
        font-family: 'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande', 'Lucida Sans Unicode', Geneva, Verdana, sans-serif;
        font-size: 10px;
        width: 100%;
    }
</style>

@*<style>
        .main-flex {
            display: flex;
            flex-direction: column;
            flex-wrap: wrap;
            width: 100%;
            justify-content: space-evenly;
            align-items: center;
            font-family: 'Roboto', sans-serif;
            font-weight: 300;
        }

        #flex-ele {
            display: flex;
            flex-direction: row;
            flex-wrap: nowrap;
            justify-content: space-evenly;
        }

        .img-container {
            margin-right: 7px;
            display: inline;
        }

        .img {
            float: left;
            margin-right: 4%;
            border-radius: 70%;
            overflow: hidden;
            display: inline;
            /*max-width: 100%;

            overflow: hidden;
            position: relative;*/
            width: 100%;
        }

        .second {
            float: left;
            width: 40%;
            display: inline;
            margin-top: 2em;
        }

        .name {
            color: #00a88d;
            font-size: 20px;
            font-weight: bold;
            letter-spacing: .06em;
            margin-bottom: .5em;
            display: inline;
        }

        .discription {
            margin-bottom: 1em;
            text-align: start;
            width: 200px;
            display: inline;
        }

        .button {
            position: relative;
            /*padding: 9px 30px 8px 20px;*/
            border: none;
            border-width: 2px;
            border-color: #bbd876;
            border-style: solid;
            border-width: 0 0 0 2px;
            letter-spacing: .05em;
            overflow: hidden;
            transition: border-color .2s;
            z-index: 0;
            text-align: center;
            font-size: 1.8vw;
            background-color: beige;
            color: forestgreen;
            display: inline;
        }
    </style>*@
=======
<h2>List Of Programs</h2>

<div class="main-box">

    @foreach (var item in Model)
    {
        <div class="flex-data">
            <div class="flex-box">
                <img src="~/program-images/@item.Name/pic/logo.jpg" />
            </div>
            <div class="flex-box">
                <div>
                    @Html.DisplayFor(modelItem => item.Name)
                </div>
                <div>
                    @Html.DisplayFor(modelItem => item.Discription)
                </div>
                <div>
                    @Html.DisplayFor(modelItem => item.Duration)
                </div>
                <div>
                    @Html.ActionLink("Program Details", "ProgramDetails", new { id = item.Id })
                </div>
                <div>
                    @Html.ActionLink("All Students", "StudentInProgram", new { id = item.Id })
                </div>
            </div>

        </div>
    }
</div>
@*<main>
    @foreach (var item in Model)
    {
        <section>
            <div>
                <img src="~/program-images/@item.Name/pic/logo.jpg" />
            </div>
            <div>
                @Html.DisplayFor(modelItem => item.Discription)
                <div>
                    @Html.ActionLink("Program Details", "ProgramDetails", new { id = item.Id })
                    @Html.ActionLink("All Students", "StudentInProgram", new { id = item.Id })
                </div>
            </div>
        </section>
</main>*@
>>>>>>> aeac4b5a28625e17321c66332e497c149cd6f953

