@model Resume_Portal.Models.CompleteStudentInfo
@{
    Layout = null;
}

<h3>
    @Html.ActionLink("Add Experience", "AddExperience")
</h3>
<table class="table">
    <tr>
        <th>
            @Html.DisplayName("InstituteName")
        </th>
        <th>
            @Html.DisplayName("Discription")
        </th>
        <th>
            @Html.DisplayNameFor("Start")
        </th>
        <th>
            @Html.DisplayNameFor("End")
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.InstituteName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Discription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Start)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.End)
            </td>
            <td>
                @Html.ActionLink("Delete", "DeleteExperience", new { id = item.Id })
            </td>
        </tr>
    }

</table>
